# --- Ядро веб-сервера ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# Набор корневых сертификатов. Python на Windows не читает системное
# хранилище, и без certifi загрузка ffmpeg и предпросмотр видео падают
# с «unable to get local issuer certificate» на чистой системе.
# Сюда он приходит и как зависимость других пакетов, но полагаться
# на это нельзя — фиксируем явно.
certifi

# --- Загрузчики медиа ---
# yt-dlp обновляется очень часто (сайты постоянно ломают парсеры),
# поэтому версия не закреплена и в приложении есть кнопка обновления.
yt-dlp
gallery-dl

# Решатель JavaScript-задачек YouTube. Без него сайт отвечает
# «Sign in to confirm you're not a bot» даже с правильными куками.
# Движок для него (Deno) приложение скачивает само в папку bin.
yt-dlp-ejs

# Нужна yt-dlp, чтобы вшивать обложку и теги в скачанный звук.
# Без неё загрузка аудио доходит до конца и падает на последнем шаге.
mutagen


# --- Оконный режим (необязательно, приложение работает и без него) ---
pywebview==5.3.2
